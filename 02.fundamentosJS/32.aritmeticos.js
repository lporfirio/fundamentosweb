const [a, b, c, d] = [3, 5, 1, 15]

const soma = a + b + c + d // operação binária
const subtracao = d - b
const multiplicacao = a * b
const divisao = d / a
const modulo = a % 2





//______________________________

let e = 2
e++ // postfixed - pós fixado
++e // prefixed - pré fixado
e + b // in fixed
console.log(e)
console.log(soma, subtracao, multiplicacao, divisao, modulo)